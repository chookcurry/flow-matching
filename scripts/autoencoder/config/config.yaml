data:
  dataset_id: uci_har
  scv_group_index: 0
  datasets_dir: datasets

model:
  architecure: cae
  num_channels_spect: 18
  num_channels_latent: 24
  size_spect: 32
  size_latent: 8
  embedding_dim: 32

train:
  batch_size: 128
  num_epochs: 100
  learning_rate: 1e-3
  patience: 5
  seed: 0

wandb:
  project: test # autoencoder